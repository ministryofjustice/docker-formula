{% set docker = salt['grains.filter_by']({
    'Debian': {
        'pkg': 'lxc-docker',
        'pkg_version': '1.9.1',
        'py_version': '1.10.3',
        'datadir': '',
    },
    'default': 'Debian',
}, merge=salt['pillar.get']('docker',{})) %}
